extends ../layout/main

block variables
  - var title = 'Toxin';
  - var description = 'Description for Webpack + Pug';
  - var pageClass = 'index-page';
  - var authorised = true


block script
  script(type='text/javascript').
    const foo = true;
    let bar = function() {};
    if (foo) {
    bar(1 + 5)
    }